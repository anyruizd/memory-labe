export interface Memory {
    id?: number
    name: string
    description: string
    timestamp: string
    imageUrl: string
    userId: number
}

export interface User {
    id?: number
    name: string
}